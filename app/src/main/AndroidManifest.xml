<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".widget.QuickAddActivity"
            android:exported="false"
            android:theme="@android:style/Theme.DeviceDefault.Dialog" />

        <!-- The detail sheet, opened by tapping a row in the widget. Translucent so the launcher stays
             visible behind the sheet, and so the platform paints no surface of its own (Theme.*.Dialog
             would, in Material You colours on API 31+, which is not this app's palette).
             It must NOT declare android:screenOrientation: on API 26 a translucent activity that
             requests an orientation throws "Only fullscreen opaque activities can request orientation". -->
        <activity
            android:name=".widget.TaskDetailActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Glance widget. Replaces the v1 RemoteViews collection widget (TodoWidgetProvider +
             TodoWidgetService), whose per-row taps rode a hand-rolled PendingIntent template plus
             per-row fill-in extras and failed silently on some launchers -> onReceive hit
             `getStringExtra(EXTRA_ID) ?: return` and the checkbox looked dead.
             Glance ALSO uses template + fill-in inside a LazyColumn — verified in the 1.1.1 bytecode,
             ApplyActionKt.applyAction branches on isLazyCollectionDescendant(). The difference is that
             it builds the template correctly (FLAG_MUTABLE) and binds one fill-in per view id, so a
             row can carry two independent actions: the circle ticks, the rest opens the task. -->
        <receiver
            android:name=".widget.TodoGlanceReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/todo_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
