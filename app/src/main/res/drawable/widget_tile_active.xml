<!-- The ACTIVE tile: a thin cyan ring on the same quiet surface. A ring means "current" —
     the filled cyan circle stays reserved for a completed check. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#FF12141B" />
    <stroke android:width="1dp" android:color="#FF35E0F0" />
    <corners android:radius="12dp" />
</shape>
