<!-- Active tool button: cyan ring + cyan glyph on the quiet surface. Never a solid cyan disc —
     that shape already means "a completed check" everywhere else in the product. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="oval">
    <solid android:color="#FF12141B" />
    <stroke android:width="1dp" android:color="#FF35E0F0" />
</shape>
